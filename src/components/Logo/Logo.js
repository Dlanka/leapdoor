import React from 'react';

import styled from 'styled-components';

const LogoBlock = styled.div`
    height:30px;
`

const Logo = props => (
    <LogoBlock>
        Logo
    </LogoBlock>
);

export default Logo;